{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ReMa's own interface: the local frontend in the main webview. Web pages in the browser workspace (webview `browser`) get no capability, so they cannot call any command or plugin.",
  "webviews": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging", "app-commands"]
}
